Silly little Rails app to help track what props one needs for photos they want to recreate.

To get setup:
 * Create a database
 * configure `config/database.yml`
 * run `rake db:migrate`
 * start the server, or setup pow
